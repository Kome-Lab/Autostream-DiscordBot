services:
  discord-bot:
    build: .
    env_file: .env.example
    environment:
      AUTOSTREAM_NODE_CONFIG: /etc/autostream-discord-bot/config.yml
      CREDENTIALS_DIRECTORY: /run/autostream-credentials
    configs:
      - source: node-listener
        target: /run/autostream-credentials/node-listener.json
    ports:
      - "127.0.0.1:${AUTOSTREAM_DISCORD_BOT_PORT:-8083}:${AUTOSTREAM_DISCORD_BOT_CONTAINER_PORT:-8080}"
    volumes:
      - ./config/config.yml:/etc/autostream-discord-bot/config.yml:ro

configs:
  node-listener:
    content: |
      {"schema_version":2,"service_type":"discord_bot","bind_address":"0.0.0.0:${AUTOSTREAM_DISCORD_BOT_CONTAINER_PORT:-8080}","config_revision":${AUTOSTREAM_CONFIG_REVISION:?AUTOSTREAM_CONFIG_REVISION is required}}
